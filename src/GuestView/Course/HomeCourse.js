import React from 'react';
import CardCourse from './CardCourse'
const HomeCourse = ({property}) => {
    return (
        <div>
            <CardCourse property={property} />
        </div>
    );
}

export default HomeCourse;